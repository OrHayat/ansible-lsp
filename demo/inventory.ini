# Inventory for the multi-host demo (multi_host_vars.yml). Defines which hosts are in which
# group — that mapping is what decides which group_vars apply to which host at runtime.
#
# The LSP does NOT read this file: locating a separate inventory is exactly the thing we
# don't guess. It's here for you, the human, to see why app_port has no single value.
[webservers]
web01
web02

[db]
db01
