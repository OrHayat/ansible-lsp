# T-062: a second folder, so the picker has two to choose between and the choice visibly
# changes the answer. Same names as prod, different values.

[webservers]
web01 host_line_ip=10.2.0.11

[webservers:vars]
ntp_server=10.2.0.1
app_tier=staging-dir
