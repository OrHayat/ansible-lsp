{
  "name": "ansible-lsp-client",
  "displayName": "Ansible LSP (local)",
  "description": "Go-to-definition for Ansible file references, backed by a Rust language server.",
  "publisher": "community-local",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.75.0"
  },
  "main": "./src/extension.js",
  "activationEvents": [
    "onLanguage:ansible",
    "onLanguage:yaml"
  ],
  "contributes": {
    "commands": [
      {
        "command": "ansibleLsp.restart",
        "title": "Restart Server",
        "category": "Ansible LSP"
      }
    ],
    "configuration": {
      "title": "Ansible LSP",
      "properties": {
        "ansibleLsp.inlayHints.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show inline hints describing what a `when:` does on a run with no extra vars. Diagnostics are unaffected."
        },
        "ansibleLsp.inlayHints.explanations": {
          "type": "boolean",
          "default": true,
          "description": "Show the hover tooltip explaining what a `when:` on `import_playbook` does. Only import hints carry one \u2014 a plain task's hint already states the answer."
        },
        "ansibleLsp.serverPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the ansible-lsp binary. Defaults to ../target/release/ansible-lsp relative to this extension."
        },
        "ansibleLsp.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Log LSP traffic to the 'Ansible LSP' output channel."
        }
      }
    }
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
